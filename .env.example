# SEC asks automated clients to identify a real person or organization and contact email.
# Example format only; replace with your own information locally.
SEC_USER_AGENT="Your Name your.email@example.com"

# Obtain a free API key from FRED and keep the real value only in the ignored .env file.
FRED_API_KEY="replace-with-your-fred-api-key"
